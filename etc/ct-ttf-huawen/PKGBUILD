pkgname=ct-ttf-huawen
pkgver=1.1
pkgrel=1
pkgdesc="custom fonts"
arch=(any)
depends=(fontconfig xorg-font-utils)
install=${pkgname}.install

package() {
  install -d "$pkgdir/usr/share/fonts/TTF"
  install -m644 "$srcdir/$pkgname/"*.ttf "$pkgdir/usr/share/fonts/TTF/"
}
